services:
  backend:
    build: .
    ports:
      - 8501:8501
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
    develop:
      watch:
        - path: .
          action: sync+restart
          target: /src
